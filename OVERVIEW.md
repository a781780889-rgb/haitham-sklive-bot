# 🎯 نظام الأعذار الطبية المتكامل - نظرة عامة

## 📦 ما تم تطويره

نظام احترافي متكامل يربط **بوت تيليجرام** بـ **موقع ويب** مع:

✅ **واجهة API آمنة** - للتواصل بين البوت والموقع
✅ **موقع ويب للاستعلام** - مع عرض PDF مباشر
✅ **حذف تلقائي** - بعد 90 يوماً من تاريخ الإنشاء
✅ **تشفير وأمان** - حماية شاملة للبيانات
✅ **سجلات مفصلة** - تتبع جميع العمليات
✅ **جاهز للإنتاج** - مع Nginx و systemd

---

## 🏗️ الهيكل المعماري

```
[تيليجرام] ←→ [بوت محسّن] ←→ [API Server] ←→ [قاعدة البيانات]
                                      ↕
                              [موقع الويب] ←→ [المستخدمون]
                                      
                              [خدمة التنظيف التلقائي]
```

---

## 📁 الملفات الرئيسية الجديدة

### 🔧 الملفات الأساسية:
- `api_server.py` - خادم API (المنفذ 5001)
- `web_enhanced.py` - موقع الويب (المنفذ 5000)
- `bot_enhanced.py` - بوت تيليجرام المحسّن
- `bot_api_integration.py` - طبقة التكامل
- `auto_cleanup.py` - خدمة التنظيف التلقائي

### 📚 التوثيق:
- `README_ENHANCED.md` - الدليل الشامل (150+ صفحة)
- `QUICKSTART.md` - البدء السريع (5 دقائق)
- `UPDATES_V2.md` - ملخص التحديثات

### ⚙️ للإنتاج:
- `nginx_config.conf` - تكوين Nginx + SSL
- `systemd_services.txt` - خدمات systemd
- `.env.example` - مثال الإعدادات

### 🚀 سكريبتات التشغيل:
- `install.sh` - التثبيت التلقائي
- `start_all.sh` - تشغيل جميع الخدمات

---

## 🎯 تحقيق جميع المتطلبات

### ✅ 1. ربط البوت بالموقع
- **API Server** منفصل ومستقل
- **مفاتيح API** للأمان
- **تخزين البيانات** في قاعدة بيانات مشتركة

### ✅ 2. واجهة استعلام بالموقع
- **صفحة ويب تفاعلية** بتصميم حديث
- **إدخال رمز الإجازة + رقم الهوية**
- **عرض البيانات** بشكل منظم

### ✅ 3. عرض PDF مباشرة
- **معاينة PDF** داخل المتصفح
- **تحميل سلس** من API Server
- **عرض آمن** مع التحقق من الصلاحيات

### ✅ 4. حذف تلقائي بعد 90 يوم
- **خدمة تنظيف** تعمل في الخلفية
- **جدولة تلقائية** (كل يوم الساعة 3 صباحاً)
- **حذف السجلات والملفات** المنتهية

### ✅ 5. الأمان والخصوصية
- **تشفير مفاتيح API** (SHA-256)
- **سجل وصول كامل** لكل استعلام
- **تحقق مزدوج** (رمز الإجازة + رقم الهوية)
- **حماية من الوصول غير المصرح**

---

## 🚀 كيفية الاستخدام

### التثبيت (مرة واحدة):
```bash
chmod +x install.sh
./install.sh
```

### التكوين:
```bash
nano .env
# قم بتحديث BOT_TOKEN
```

### التشغيل:
```bash
./start_all.sh
```

### الوصول:
- **الموقع:** http://localhost:5000
- **البوت:** @YourBotName على تيليجرام

---

## 📊 سير العمل

### إنشاء إجازة:
1. المستخدم يتحدث مع البوت
2. يملأ البيانات المطلوبة
3. البوت ينشئ PDF
4. البوت يحفظ في API Server
5. المستخدم يحصل على:
   - ملف PDF
   - رمز إجازة (GSL######)
   - رابط الاستعلام

### الاستعلام:
1. المستخدم يفتح الموقع
2. يدخل رمز الإجازة + رقم الهوية
3. الموقع يرسل طلب لـ API
4. API يتحقق ويرجع البيانات
5. الموقع يعرض:
   - معلومات الإجازة
   - معاينة PDF مباشرة

### التنظيف التلقائي:
1. الخدمة تعمل في الخلفية
2. كل يوم الساعة 3 صباحاً
3. تحذف السجلات الأقدم من 90 يوم
4. تحذف ملفات PDF المرتبطة
5. تسجل العملية في السجلات

---

## 🔒 الأمان

### المستويات الأمنية:
1. **مفاتيح API** - للوصول إلى API Server
2. **رمز الإجازة** - فريد لكل إجازة
3. **رقم الهوية** - تحقق إضافي
4. **سجل الوصول** - تتبع جميع المحاولات
5. **حذف تلقائي** - حماية الخصوصية

---

## 📈 الإحصائيات والمراقبة

### السجلات:
```
logs/api_server.log     - سجلات API
logs/web_server.log     - سجلات الموقع
logs/bot.log            - سجلات البوت
logs/auto_cleanup.log   - سجلات التنظيف
```

### أوامر المشرف:
```
/api_stats   - إحصائيات النظام
/cleanup     - تنظيف يدوي
/check_api   - فحص API
/website     - رابط الموقع
```

---

## 🎨 الميزات التقنية

### Backend:
- **Python 3.8+**
- **Flask** - خوادم الويب و API
- **SQLite** - قاعدة البيانات
- **python-telegram-bot** - البوت
- **ReportLab** - توليد PDF

### Frontend:
- **HTML5 + CSS3**
- **JavaScript** (Vanilla)
- **تصميم متجاوب**
- **عارض PDF مدمج**

### DevOps:
- **Bash scripts** - التشغيل والتثبيت
- **Systemd** - خدمات لينكس
- **Nginx** - خادم الإنتاج
- **Let's Encrypt** - شهادات SSL

---

## 📦 ما يشمله الملف المضغوط

```
enhanced_bot.zip
├── api_server.py              # خادم API
├── web_enhanced.py            # موقع الويب
├── bot_enhanced.py            # البوت المحسّن
├── bot_api_integration.py     # طبقة التكامل
├── auto_cleanup.py            # التنظيف التلقائي
├── database.py                # قاعدة البيانات
├── pdf_gen.py                 # توليد PDF
├── bot.py                     # البوت الأصلي
├── web.py                     # الموقع الأصلي
├── install.sh                 # سكريبت التثبيت
├── start_all.sh               # سكريبت التشغيل الشامل
├── requirements.txt           # المتطلبات
├── .env.example               # مثال الإعدادات
├── nginx_config.conf          # تكوين Nginx
├── systemd_services.txt       # خدمات systemd
├── README_ENHANCED.md         # الدليل الشامل
├── QUICKSTART.md              # البدء السريع
├── UPDATES_V2.md              # التحديثات
├── OVERVIEW.md                # هذا الملف
├── templates/                 # قوالب PDF
├── fonts/                     # الخطوط العربية
├── logos/                     # الشعارات
└── (ملفات البوت الأصلية)
```

---

## ✨ الخلاصة

تم تطوير نظام **احترافي ومتكامل** يحقق **جميع المتطلبات**:

✅ ربط آمن بين البوت والموقع
✅ واجهة استعلام حديثة
✅ عرض PDF مباشر
✅ حذف تلقائي بعد 90 يوم
✅ أمان وخصوصية شاملة
✅ سجلات مفصلة
✅ سهولة التشغيل والصيانة
✅ جاهز للإنتاج

---

## 🎉 ابدأ الآن!

```bash
# فك الضغط
unzip enhanced_bot.zip
cd enhanced_bot

# التثبيت
./install.sh

# التشغيل
./start_all.sh
```

**للمزيد من التفاصيل:** راجع `README_ENHANCED.md`

---

**© 2026 - نظام متكامل احترافي**
