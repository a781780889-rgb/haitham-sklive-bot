# 🗄️ دليل إعداد قاعدة البيانات الدائمة على Railway
## المشكلة والحل

| المشكلة | السبب | الحل |
|---|---|---|
| البيانات تُمسح عند إعادة النشر | SQLite يُحفظ على القرص المؤقت | PostgreSQL على Railway |
| الشعارات تختفي | ملفات مخزّنة على القرص | حفظها مباشرة في قاعدة البيانات |
| القوالب تختفي | ملفات مخزّنة على القرص | حفظها مباشرة في قاعدة البيانات |

---

## الخطوة 1 — إضافة PostgreSQL على Railway

1. افتح مشروعك على **railway.app**
2. اضغط **+ New** → **Database** → **Add PostgreSQL**
3. انتظر حتى يتم إنشاء قاعدة البيانات (30 ثانية تقريباً)
4. اضغط على قاعدة البيانات الجديدة → تبويب **Variables**
5. انسخ قيمة `DATABASE_URL`

---

## الخطوة 2 — إضافة المتغير لخدمة البوت

1. اضغط على خدمة البوت (ليس قاعدة البيانات)
2. اذهب إلى تبويب **Variables**
3. اضغط **New Variable**
4. أضف:
   ```
   DATABASE_URL = postgresql://postgres:xxxx@xxx.railway.internal:5432/railway
   ```
   (الصق القيمة التي نسختها من قاعدة البيانات)

---

## الخطوة 3 — التحقق من باقي المتغيرات

تأكد أن هذه المتغيرات موجودة في **Variables** للبوت:

```
BOT_TOKEN       = توكن_البوت_من_BotFather
ADMIN_IDS       = معرفك_من_userinfobot
ADMIN_PASS      = كلمة_السر_للوحة_الإدارة
IBAN            = رقم_الآيبان
PAYMENT_NAME    = اسم_صاحب_الحساب
STC_NUMBER      = رقم_STC
DATABASE_URL    = [من الخطوة 2]
```

---

## الخطوة 4 — إعادة النشر

بعد إضافة `DATABASE_URL`:

1. اضغط على خدمة البوت
2. اضغط **Deploy** أو **Redeploy**
3. شاهد الـ Logs — يجب أن تظهر:
   ```
   🐘 db_adapter: تم تفعيل وضع PostgreSQL
   ✅ file_storage: جدول file_blobs جاهز
   ```

---

## الخطوة 5 — إعادة رفع الشعارات والقوالب (مرة واحدة فقط)

بعد أول نشر بنجاح، ارفع شعارات المستشفيات والقوالب مجدداً من البوت.  
**من الآن فصاعداً لن تُمسح أبداً** — تُخزَّن مباشرة في PostgreSQL.

---

## ما الذي يُحفظ الآن بشكل دائم؟

| البيانات | مكان الحفظ |
|---|---|
| المستخدمون وأرصدتهم | PostgreSQL |
| الطلبات والإجازات | PostgreSQL |
| المستشفيات والأطباء | PostgreSQL |
| **شعارات المستشفيات** | **PostgreSQL (file_blobs)** |
| **قوالب PDF** | **PostgreSQL (file_blobs)** |
| الإعدادات | PostgreSQL |
| المعاملات المالية | PostgreSQL |
| أكواد الشحن | PostgreSQL |

---

## استكشاف الأخطاء

### ❌ الخطأ: `psycopg2 not found`
```
pip install psycopg2-binary
```
(موجود في requirements.txt بالفعل)

### ❌ الخطأ: `could not connect to server`
- تأكد أن `DATABASE_URL` صحيح
- تأكد أن Railway PostgreSQL يعمل (ليس في حالة Sleep)

### ❌ البيانات القديمة لا تظهر
- قاعدة البيانات الجديدة فارغة — هذا طبيعي
- أعد إضافة المستشفيات والأطباء من لوحة الإدارة
- أو استخدم أمر الترحيل التلقائي (انظر أدناه)

---

## ترحيل البيانات من SQLite القديم (اختياري)

إذا كان لديك `bot_data.db` قديم وتريد نقل بياناته:

```python
# شغّل هذا مرة واحدة فقط على جهازك المحلي
python3 migrate_to_postgres.py
```

(الملف غير مدرج — تواصل مع المطور إذا احتجته)
