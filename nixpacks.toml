[phases.setup]
nixPkgs = [
  "cairo",
  "pango",
  "gdk-pixbuf",
  "libffi",
  "gobject-introspection",
  "harfbuzz",
  "fontconfig",
  "zlib"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3 bot.py"
