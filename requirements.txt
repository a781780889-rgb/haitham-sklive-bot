# ══════════════════════════════════════════════════════════════════
# requirements.txt — Local AI Edition (بدون API خارجي)
# ══════════════════════════════════════════════════════════════════
# ✅ كل المكتبات مجانية ومحلية
# ✅ تعمل Offline بالكامل
# ✅ بدون anthropic / openai / أي خدمة مدفوعة

# ── البوت الأساسي ───────────────────────────────────────────────
python-telegram-bot==20.7

# ── PDF وتقارير ─────────────────────────────────────────────────
pypdf>=4.0
reportlab>=4.0
Pillow>=10.0

# ── دعم العربية في PDF ──────────────────────────────────────────
arabic-reshaper>=3.0
python-bidi>=0.4

# ── ويب وAPI ────────────────────────────────────────────────────
flask>=3.0
flask-cors>=4.0
gunicorn>=21.0

# ── قاعدة البيانات ───────────────────────────────────────────────
psycopg2-binary>=2.9

# ── أدوات عامة ──────────────────────────────────────────────────
python-dotenv>=1.0
cryptography>=41.0
numpy>=1.24
qrcode

# ── تحويل التاريخ الهجري (اختياري — يُحسّن دعم التقويم الهجري) ──
# hijri-converter>=2.3

# ══════════════════════════════════════════════════════════════════
# ملاحظات:
# - لا حاجة لـ anthropic أو openai
# - لا حاجة لـ transformers أو torch (ثقيلة جداً)
# - المحرك المحلي يعمل بـ Python stdlib + regex + difflib فقط
# - إذا أردت تحسين دعم Hijri أزل التعليق عن hijri-converter
# ══════════════════════════════════════════════════════════════════
