# requirements_enhanced.txt — المكتبات المطلوبة للنظام الذكي v3
# ════════════════════════════════════════════════════════════════

# ── المكتبات الأصلية ──────────────────────────────────────────
python-telegram-bot==20.7
Pillow>=10.0.0
reportlab>=4.0.0
qrcode[pil]>=7.4.2
python-dotenv>=1.0.0
psutil>=5.9.0

# ── قواعد البيانات ────────────────────────────────────────────
# SQLite مدمج مع Python — لا يحتاج تثبيت
# PostgreSQL (Railway)
psycopg2-binary>=2.9.9

# ── معالجة النصوص والـ NLP ─────────────────────────────────────
# تحويل التاريخ الهجري (اختياري — يوجد fallback رياضي)
hijri-converter>=2.3.1

# ── لا يوجد مكتبات NLP ثقيلة — النظام يعمل بخوارزميات خفيفة ──
# الخوارزميات المستخدمة:
#   • Levenshtein Distance — مدمجة في الكود
#   • Token Jaccard Similarity — مدمجة في الكود
#   • Arabic Normalization — مدمجة في duplicate_detector.py

# ── ويب سيرفر (اختياري) ───────────────────────────────────────
flask>=3.0.0
gunicorn>=21.0.0

# ── أدوات مساعدة ──────────────────────────────────────────────
aiofiles>=23.0.0
