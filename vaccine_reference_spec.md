# مواصفات المرجع البصري لشهادة التطعيم

## الصفحة

المرجع `شهادةتطعيم.pdf` صفحة واحدة بمقاس **842 × 1190 نقطة**، أي **A3 Portrait**. الاتجاه عمودي، ولا توجد صفحات إضافية.

## الخطوط المستخرجة

يحتوي المرجع على خط عربي مضمّن باسم `NotoSansArabic-Bold`، وخط لاتيني مضمّن باسم غير واضح `RB-Regular`، إضافة إلى `Times-Bold` للعناوين. سيتم استخدام الخطوط المضمّنة في الخلفية كما هي، واستخدام Noto Sans Arabic وTimes-Roman للبيانات المتغيرة مع الحفاظ على أوزان قريبة.

## بنية العناصر

الجزء العلوي يحتوي شعار وزارة الصحة والزخرفة الخضراء، ثم العنوان المركزي «شهادة التطعيم / Vaccination Certificate». جدول الهوية يقع تقريبًا بين x=47 و795، ومن y=735 إلى 878 من أسفل الصفحة، بخمسة صفوف: الاسم، الهوية/الإقامة، الميلاد، الجواز، الجنسية. الشريط الأخضر السفلي يقع تقريبًا بين x=47 و795 وy=665 إلى 708، ويضم خمسة أعمدة: رقم التشغيلة، سبب التطعيم، العمر، تاريخ التطعيم، نوع التطعيم.

البيانات المتغيرة في جدول الهوية لها عمود إنجليزي أيسر تقريبًا x=198..479 وعمود عربي x=479..655، بينما عناوين الحقول ثابتة على الجانبين. أسفل الشريط توجد صفوف سجل التطعيم، وتظهر البيانات في نطاق تقريبي y=585..665. يحتوي المرجع على QR في المنطقة الوسطى السفلية تقريبًا x=385..455 وy=350..430، ورقم شهادة أسفله/أعلاه، مع نصوص تحقق وروابط ثابتة.

سيُستخدم المرجع المرئي كخلفية ثابتة بعد تنظيف بيانات العينة، ثم تُرسم بيانات المستخدم في مواضعها، مع دعم صفوف متعددة للتطعيمات إن وُجدت.

## نتيجة المقارنة البصرية

النسخة التجريبية النهائية أصبحت A3 عمودية بمقاس 842×1190 نقطة، مع الحفاظ على الشعار والعنوان والزخرفة والنصوص السفلية من المرجع. ظهرت قيم الهوية داخل خلاياها، وقيم صف التطعيم أسفل الشريط الأخضر، ورقم الشهادة والـQR في المنطقة المرجعية. تم استخدام الإحداثيات المستخرجة من صناديق النص في المرجع بدل القياسات التقريبية السابقة.
